node_modules/
.next/
out/
*.tsbuildinfo
next-env.d.ts
.DS_Store
.env*.local

# Mirrored card art — regenerate with `npm run ingest:images`
public/cards/

# Derived from public/cards by `npm run build:cdn`, uploaded to Cloudflare Pages
cdn/

# Parked by build-static.mjs while the export runs
.art-route-parked/

# A Supabase dump, if one was taken locally. This repository is public and
# organizer_requests holds text people wrote about themselves — the workflow keeps
# it as a private Actions artifact, and a local run must not be one `git add -A`
# away from publishing it.
backup/
